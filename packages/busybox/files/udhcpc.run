#!/usr/local/bin/ysh
# runit service: udhcpc
# Wait for the ethernet interface, bring it up, then request a DHCP lease.

while ! ip link show eth0 >/dev/null 2>&1 {
    sleep 1
}

ip link set eth0 up

exec udhcpc -f -i eth0 -s /etc/udhcpc.sh
