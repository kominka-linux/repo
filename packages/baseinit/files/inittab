::sysinit:/usr/lib/init/rc.boot
::restart:/sbin/init
::shutdown:/usr/lib/init/rc.shutdown

# runit service supervision
::respawn:runsvdir -P /var/service 'log: ................................................................................................................................................................................................................................................................'

# Virtual terminal gettys — id prefix triggers seed init's TTY setup
hvc0::respawn:/usr/local/bin/autologin
tty1::respawn:/bin/getty 38400 tty1
