#!/usr/local/bin/ysh

var name    = 'liveiso'
var ver     = '1'
var rel     = '1'
var deps    = ['core', 'e2fsprogs', 'dosfstools', 'wifi', 'zig', 'make', 'bison', 'perl', 'iwlwifi-firmware']
var mkdeps  = []

var sources = []
var checksums = []

proc build(dest) {
    # metapackage — no build step
    true
}
