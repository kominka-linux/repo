#!/usr/local/bin/ysh

var name    = 'm4'
var ver     = '1.4.21'
var rel     = '4'
var deps    = ['musl']
var mkdeps  = ['zig', 'make']

var sources = ['https://ftp.gnu.org/gnu/m4/m4-VERSION.tar.xz']
var checksums = ['f25c6ab51548a73a75558742fb031e0625d6485fe5f9155949d6486a2408ab66']

proc build(dest) {
    var zig_bin = $(command -v zig)

    # EGREP: busybox egrep may not accept -e flag that autoconf checks for.
    # busybox egrep rejects -e; autoconf's AC_PROG_EGREP requires egrep -e to work.
    # Create a wrapper that accepts -e by forwarding to grep -E.
    printf '#!/bin/sh\nexec grep -E "$@"\n' > /tmp/egrep_wrap
    chmod +x /tmp/egrep_wrap
    env CC="$zig_bin cc" PATH="/tmp:$PATH" EGREP=/tmp/egrep_wrap ./configure \
        --prefix=/usr

    # Fix gnulib _GL_ATTRIBUTE_NODISCARD incompatibility with newer GCC.
    find lib -name '*.h' -exec sed -i \
        's/^\(# *define _GL_ATTRIBUTE_NODISCARD\) .*/\1/' {} +

    make
    make DESTDIR=$dest install
}
