# /etc/resolv.conf — DNS resolver configuration.
# Managed by the network configuration system (dhcpcd, NetworkManager, etc.)
# Replace with your own nameservers or let DHCP populate this file.
nameserver 1.1.1.1
nameserver 8.8.8.8
