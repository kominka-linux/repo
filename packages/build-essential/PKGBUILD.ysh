#!/usr/local/bin/ysh

var name    = 'build-essential'
var ver     = '1.0.0'
var rel     = '3'
var deps    = ['core', 'zig', 'linux', 'zlib', 'm4', 'make', 'bison', 'samurai', 'muon', 'cmake', 'git', 'pkgconf', 'binutils']
var mkdeps  = []

var sources = []
var checksums = []

proc build(dest) {
    # Metapackage — no build step. Dependencies are pulled in via 'depends'.
    builtin true
}
