#!/usr/local/bin/ysh

var name    = 'build-essential'
var ver     = '1.0.0'
var rel     = '1'
var deps    = ['core', 'zig', 'linux-headers', 'zlib', 'bzip2', 'xz', 'm4', 'make', 'bison', 'samurai', 'cmake', 'git', 'pkgconf']
var mkdeps  = []

var sources = []
var checksums = []

proc build(dest) {
    # Metapackage — no build step. Dependencies are pulled in via 'depends'.
    true
}
